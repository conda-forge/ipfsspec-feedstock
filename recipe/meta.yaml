{% set name = "ipfsspec" %}
{% set version = "0.6.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/ipfsspec-{{ version }}.tar.gz
  sha256: 1c6c2e569560a038eaa9b8a9263d78eb00d0a7d97edbbfdeb872273d687545c0

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python {{ python_min }}
    - pip
  run:
    - python >={{ python_min }}
    - aiohttp
    - aiohttp-retry
    - fsspec >=0.9.0
    - multiformats
    - requests

test:
  requires:
    - python {{ python_min }}
    - pip
  commands:
    - pip check
  imports:
    - ipfsspec

about:
  home: https://github.com/fsspec/ipfsspec
  license: MIT
  license_file: LICENSE
  summary: A readonly implementation of fsspec for IPFS

extra:
  recipe-maintainers:
    - raybellwaves
    - lkluft
